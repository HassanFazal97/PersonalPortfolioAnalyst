.git
.venv
venv
__pycache__
*.pyc
*.pyo
.pytest_cache
.ruff_cache
*.egg-info
.env
.DS_Store
tests
macworker
PROJECT_SPEC.md
# Non-Python trees. The Dockerfile copies `app` and `scripts` explicitly, so
# none of these could reach the image anyway — but node_modules in the build
# context makes every Railway deploy upload them.
mobile
ad
docs
evals

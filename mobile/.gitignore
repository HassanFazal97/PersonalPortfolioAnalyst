# Expo / React Native
node_modules/
.expo/
dist/
web-build/
expo-env.d.ts

# Native projects — this app is Expo-managed with config plugins, so ios/ and
# android/ are generated by prebuild and must never be committed.
/ios
/android

# Local env (see .env.example)
.env
.env.local

# Metro / EAS
.metro-health-check*
.eas-build-local

# macOS
.DS_Store
*.pem

# Debug
npm-debug.*
yarn-debug.*
yarn-error.*
